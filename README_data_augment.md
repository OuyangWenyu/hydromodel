# 水文数据增强模块 (Hydrological Data Augmentation)

## 概述

水文数据增强模块是一个专门用于生成合成洪水事件的工具，基于最优洪水事件和单位线来生成大量的训练样本。该模块采用了机器学习领域常见的数据增强设计模式，提供了灵活、可扩展的接口。

## 主要功能

### 🌊 渐进式降雨添加
- 第一个时段的净雨生成一场洪水
- 第一个+第二个时段的净雨生成另一场洪水
- 依此类推，生成一系列不同长度的洪水事件

### 📊 多重缩放因子
- 支持对净雨应用不同的缩放因子（如0.5, 0.8, 1.2, 1.5, 2.0倍）
- 每个缩放因子与每种降雨模式组合，大幅增加样本数量

### ⏰ 时间结构保持
- 保持原始洪水事件的月份、日期、小时信息
- 年份递增，每个生成的事件对应一个新年份
- 支持自定义起始年份偏移

### 🏞️ 多流域支持
- 单个类可以处理多个流域的数据
- 每个流域可以有独立的最优事件和单位线

### 🔄 单位线卷积
- 使用单位线与净雨进行卷积计算，生成合理的径流过程
- 支持标准的10mm净雨对应单位线

## 设计模式

该模块借鉴了机器学习框架（如sklearn、imgaug）的优秀设计模式：

- **策略模式 (Strategy Pattern)**: 支持不同的数据增强策略
- **建造者模式 (Builder Pattern)**: 通过配置灵活构建增强器
- **模板方法模式 (Template Method)**: 标准的fit-transform接口
- **工厂模式 (Factory Pattern)**: 提供便捷的创建函数

## 安装和使用

### 基本使用方法

#### 使用真实数据（推荐）

```python
from hydromodel_dev.data_augment import create_real_data_augmenter

# 方式1：直接创建使用真实数据的增强器（自动加载和优化）
augmenter = create_real_data_augmenter(
    station_id="songliao_21401550",
    optimization_mode="shared",  # 或 "categorized" 
    top_n_events=5,
    min_nse_threshold=0.7,
    scaling_factors=[0.8, 1.2, 1.5, 2.0],
    verbose=True
)

# 生成增强数据（数据已自动加载和训练）
augmented_events = augmenter.transform()

# 保存结果
augmenter.save_augmented_events(augmented_events, "results/real_data_augmentation")
```

#### 从已有结果文件加载

```python
from hydromodel_dev.data_augment import create_real_data_augmenter

# 方式2：从已有的单位线优化结果加载
augmenter = create_real_data_augmenter(
    results_file="results/UH_shared_eva_output_songliao_21401550.csv",
    top_n_events=8,
    min_nse_threshold=0.8,
    scaling_factors=[0.8, 1.2, 1.5],
    verbose=True
)

# 生成增强数据
augmented_events = augmenter.transform()
```

#### 手动数据加载

```python
from hydromodel_dev.data_augment import (
    HydrologicalDataAugmenter, 
    load_real_hydrological_data
)

# 方式3：手动加载数据然后创建增强器
real_data = load_real_hydrological_data(
    station_id="songliao_21401550",
    optimization_mode="shared",
    top_n_events=10,
    min_nse_threshold=0.7,
    uh_length=24,
    verbose=True
)

# 创建增强器
augmenter = HydrologicalDataAugmenter(
    scaling_factors=[0.5, 0.8, 1.2, 1.5, 2.0],
    start_year_offset=1,
    preserve_temporal_structure=True,
    random_state=42
)

# 训练和生成
augmented_events = augmenter.fit_transform(real_data)

# 保存结果
augmenter.save_augmented_events(augmented_events, "results/augmented_events")
```

#### 使用示例数据（测试用）

```python
from hydromodel_dev.data_augment import HydrologicalDataAugmenter

# 当真实数据不可用时，可以使用示例数据进行测试
augmenter = HydrologicalDataAugmenter(
    scaling_factors=[0.8, 1.2, 1.5],
    use_real_data=False,  # 不自动加载真实数据
    verbose=True
)

# 手动准备示例数据
data = {
    'optimal_events': [
        {
            'P_eff': np.array([15.2, 8.5, 3.2, 1.1, 0.0]),  # 净雨
            'Q_obs_eff': np.array([2.5, 12.8, 18.3, 15.6, 8.9, 4.2, 1.8]),  # 直接径流
            'filepath': 'event_19940816.csv',
            'peak_obs': 18.3,
        },
        # ... 更多最优事件
    ],
    'unit_hydrographs': {
        'event_19940816.csv': np.array([0.0, 1.2, 3.8, 5.2, 4.1, 2.3, 0.8]),
        # ... 对应的单位线
    }
}

# 训练和生成
augmented_events = augmenter.fit_transform(data)
```

### 使用工厂函数

```python
from hydromodel_dev.data_augment import create_hydrological_augmenter

config = {
    'scaling_factors': [0.5, 0.8, 1.2, 1.5, 2.0],
    'start_year_offset': 1,
    'preserve_temporal_structure': True,
    'random_state': 42
}

augmenter = create_hydrological_augmenter(config)
augmented_events = augmenter.fit_transform(data)
```

### 多流域处理

```python
from hydromodel_dev.data_augment import augment_multiple_watersheds

watershed_data = {
    'watershed_A': data_A,
    'watershed_B': data_B,
    # ... 更多流域
}

results = augment_multiple_watersheds(watershed_data, config)
```

## 运行示例

项目提供了完整的示例脚本：

```bash
# 运行真实数据增强演示脚本（推荐）
uv run python scripts/run_data_augmentation.py
```

该脚本将演示：
- 🔥 **真实数据自动加载增强**：使用共享单位线优化
- 🔥 **真实数据分类增强**：使用分类单位线优化
- 📂 **从结果文件加载**：使用已有的优化结果
- 🔧 **手动数据加载**：完全控制数据加载过程
- 🎯 **样本数据回退**：当真实数据不可用时的测试方案

### 快速开始

如果您已经有松辽河数据和配置，最简单的使用方式：

```python
from hydromodel_dev.data_augment import create_real_data_augmenter

# 一行代码创建增强器并生成数据
augmenter = create_real_data_augmenter(verbose=True)
augmented_events = augmenter.transform()

print(f"生成了 {len(augmented_events)} 个增强洪水事件！")
```

## 输出格式

### 增强事件结构

每个增强事件包含以下字段：

```python
{
    'P_eff': np.array,              # 增强后的净雨序列
    'Q_obs_eff': np.array,          # 生成的径流序列  
    'Q_sim': np.array,              # 模拟径流（同Q_obs_eff）
    'filepath': str,                # 新事件文件名
    'peak_obs': float,              # 洪峰流量
    'augmentation_metadata': {      # 增强元数据
        'source_event': str,        # 源事件名称
        'scale_factor': float,      # 缩放因子
        'periods_used': int,        # 使用的时段数
        'generation_method': str,   # 生成方法
        'sample_id': int,           # 样本ID
        'is_augmented': bool        # 是否为增强样本
    },
    'temporal_info': {              # 时间信息
        'year': int,                # 年份
        'month': int,               # 月份
        'day': int,                 # 日期
        'date_string': str          # 日期字符串
    }
}
```

### 文件输出

- **CSV格式**: 每个事件保存为独立的CSV文件，包含元数据注释
- **Pickle格式**: 所有事件保存在一个pickle文件中
- **摘要文件**: 包含所有增强事件统计信息的CSV文件

## 配置参数

### 基本增强参数

| 参数                          | 类型        | 默认值                    | 说明             |
| ----------------------------- | ----------- | ------------------------- | ---------------- |
| `scaling_factors`             | List[float] | [0.5, 0.8, 1.2, 1.5, 2.0] | 净雨缩放因子列表 |
| `start_year_offset`           | int         | 1                         | 起始年份偏移量   |
| `preserve_temporal_structure` | bool        | True                      | 是否保持时间结构 |
| `random_state`                | int         | None                      | 随机种子         |

### 真实数据加载参数

| 参数                | 类型  | 默认值              | 说明                                  |
| ------------------- | ----- | ------------------- | ------------------------------------- |
| `use_real_data`     | bool  | False               | 是否自动加载真实数据                  |
| `station_id`        | str   | "songliao_21401550" | 站点ID                                |
| `optimization_mode` | str   | "shared"            | 单位线优化模式：shared 或 categorized |
| `top_n_events`      | int   | 10                  | 提取的最优事件数量                    |
| `min_nse_threshold` | float | 0.7                 | NSE最小阈值                           |
| `uh_length`         | int   | 24                  | 共享模式下的单位线长度                |
| `results_file`      | str   | None                | 现有结果文件路径（可选）              |
| `data_path`         | str   | None                | 数据目录路径（可选）                  |

### 单位线优化参数

| 参数                    | 类型  | 默认值  | 说明                 |
| ----------------------- | ----- | ------- | -------------------- |
| `smoothing_factor`      | float | 0.1     | 平滑性惩罚权重因子   |
| `peak_violation_weight` | float | 10000.0 | 单峰违反惩罚权重因子 |

## 真实数据使用指南

### 🔥 推荐工作流程

1. **准备环境**：确保 `hydrodatasource` 正确配置，松辽河数据可访问
2. **选择优化模式**：
   - `"shared"`：所有事件使用同一个单位线（适合一致性较好的流域）
   - `"categorized"`：按洪峰大小分类，每类使用独立单位线（适合变化较大的流域）
3. **设置筛选条件**：通过 `min_nse_threshold` 和 `top_n_events` 控制数据质量
4. **运行增强**：一键生成大量高质量增强样本

### 📊 数据质量控制

- **NSE阈值筛选**：只使用NSE ≥ 0.7的高质量事件作为增强基础
- **自动排序**：按NSE降序选取最优事件
- **多模式支持**：共享模式确保一致性，分类模式捕获多样性

### 🎯 实际应用示例

从实际测试结果来看，该系统能够：
- 从28个原始洪水事件中筛选出5个最优事件（NSE: 0.812-0.903）
- 生成387个增强事件（共享模式）或348个增强事件（分类模式）
- 自动保持时间结构（保留月日信息，年份递增）
- 提供完整的溯源信息（源事件、缩放因子、使用时段等）

## 生成样本数量计算

对于每个最优事件，生成的样本数量为：
```
样本数 = 有效降雨时段数 × 缩放因子数量
```

### 真实数据示例

基于实际运行结果：
- **共享模式**：5个最优事件 → 387个增强事件（平均每个源事件生成约77个样本）
- **分类模式**：4个最优事件 → 348个增强事件（平均每个源事件生成约87个样本）
- **手动模式**：3个最优事件 → 166个增强事件（平均每个源事件生成约55个样本）

### 理论计算示例

- 3个最优事件
- 每个事件平均4个有效降雨时段  
- 5个缩放因子

总生成样本数 = 3 × 4 × 5 = 60个增强事件

## 应用场景

### 📚 机器学习训练
- 为深度学习模型提供大量训练样本
- 提高模型泛化能力
- 解决水文数据稀缺问题

### 🔬 模型验证
- 生成测试数据集
- 评估模型在不同场景下的性能
- 敏感性分析

### 📈 统计分析
- 增加样本量提高统计推断的可靠性
- 研究不同降雨模式的影响
- 洪水频率分析

## 技术特点

### 🎯 精确性
- 基于物理过程的单位线卷积
- 保持水文学合理性
- 可追溯的生成过程

### ⚡ 高效性
- 向量化计算，处理速度快
- 内存友好的设计
- 支持批量处理

### 🔧 可扩展性
- 模块化设计，易于扩展
- 支持自定义增强策略
- 兼容现有水文建模工具

### 📊 可解释性
- 详细的元数据记录
- 完整的生成过程追踪
- 丰富的统计分析功能

## 注意事项

1. **数据质量**: 确保输入的最优事件和单位线数据质量良好
2. **物理合理性**: 生成的事件应进行合理性检查
3. **缩放范围**: 避免过度的缩放因子可能导致不现实的结果
4. **存储空间**: 大量增强事件可能需要较大存储空间

## 贡献指南

欢迎提交bug报告、功能请求或代码贡献。请确保：

1. 遵循项目的代码风格
2. 添加适当的测试
3. 更新相关文档
4. 提供清晰的提交信息

## 许可证

本项目采用与主项目相同的许可证。

---

## 快速开始

想要快速体验数据增强功能？运行以下命令：

```bash
# 克隆项目
git clone [project-url]
cd hydromodel_dev

# 运行示例
python scripts/run_data_augmentation_example.py
```

这将生成示例数据并展示所有主要功能！ 