# Unit Hydrograph Models

The project registers two unit-hydrograph-based models:

- `unit_hydrograph`: classic unit hydrograph model
- `categorized_unit_hydrograph`: unit hydrograph with categorized routing

Both return `qsim` and are typically used with flood-event data.

## API Reference

::: hydromodel.models.unit_hydrograph
