# UV configuration for hydromodel project
# Use Python 3.11+ as specified in pyproject.toml

# Better caching and linking
link-mode = "copy"  # Avoid hardlink issues on Windows
cache-keys = ["python_version", "platform"]